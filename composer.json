{
    "name": "phpdot/rabbitmq",
    "description": "RabbitMQ client for PHP: publish, consume, retry, dead letter, topology.",
    "type": "library",
    "license": "MIT",
    "authors": [
        {
            "name": "Omar Hamdan",
            "email": "omar@phpdot.com"
        }
    ],
    "keywords": [
        "rabbitmq",
        "amqp",
        "messaging",
        "publisher",
        "consumer",
        "retry",
        "dead-letter"
    ],
    "require": {
        "php": ">=8.5",
        "php-amqplib/php-amqplib": "^3.0",
        "phpdot/contracts": "^0.2",
        "psr/log": "^3.0",
        "symfony/console": "^8.0"
    },
    "require-dev": {
        "friendsofphp/php-cs-fixer": "^3.94",
        "phpdot/container": "^0.2",
        "phpstan/phpstan": "^2.0",
        "phpstan/phpstan-strict-rules": "^2.0",
        "phpunit/phpunit": "^13.0"
    },
    "suggest": {
        "phpdot/container": "Autowires the container-attribute services and #[Config] DTOs declared in src (the attributes stay inert until reflected, so standalone consumers don't need it installed)."
    },
    "autoload": {
        "psr-4": {
            "PHPdot\\RabbitMQ\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "PHPdot\\RabbitMQ\\Tests\\": "tests/"
        }
    },
    "scripts": {
        "test": "phpunit",
        "analyse": "phpstan analyse --memory-limit=256M",
        "cs-fix": "php-cs-fixer fix",
        "cs-check": "php-cs-fixer fix --dry-run --diff",
        "check": [
            "@test",
            "@analyse",
            "@cs-check"
        ]
    },
    "config": {
        "sort-packages": true
    },
    "support": {
        "source": "https://github.com/phpdot/rabbitmq",
        "issues": "https://github.com/phpdot/monorepo/issues"
    }
}
